% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac.R
\name{clear_overture_cache}
\alias{clear_overture_cache}
\title{Clear overtureR's catalog cache}
\usage{
clear_overture_cache()
}
\value{
The cache directory, invisibly.
}
\description{
overtureR caches what it reads from Overture's STAC catalog (the list of
types in a release, and the bounding box of every Parquet file) in memory
and on disk under \code{tools::R_user_dir("overtureR", "cache")}. Releases never
change, so the cache never goes stale, but you can clear it here. Set
\code{options(overturer_cache = FALSE)} to keep the cache in memory only, or
\code{options(overturer_cache_dir = )} to move it.
}
\examples{
\dontrun{
clear_overture_cache()
}
}
