% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sf_methods.R
\name{overture_sf_methods}
\alias{overture_sf_methods}
\alias{st_crs.overture_call}
\alias{st_bbox.overture_call}
\title{Extent and coordinate reference system without collecting}
\usage{
\method{st_crs}{overture_call}(x, ...)

\method{st_bbox}{overture_call}(obj, ...)
}
\arguments{
\item{...}{Unused, for compatibility with the generics.}

\item{obj, x}{An \code{overture_call} object, as returned by \code{\link[=open_curtain]{open_curtain()}}.}
}
\value{
\code{st_bbox()} returns an \code{sf::st_bbox()} object; \code{st_crs()} returns an
\code{sf::st_crs()} object.
}
\description{
\code{sf::st_bbox()} and \code{sf::st_crs()} methods for lazy \code{overture_call} tables,
so you can read a query's extent and coordinate reference system straight
from DuckDB, without pulling the rows into R with \code{\link[dplyr:collect]{collect()}}.
}
\details{
\code{st_bbox()} runs one \code{ST_Extent_Agg()} query over the geometry column.
\code{st_crs()} reads the coordinate reference system from DuckDB's typed
\code{GEOMETRY} column (for example \code{GEOMETRY('OGC:CRS84')} on duckdb >= 1.5),
falling back to EPSG:4326, which is what Overture stores.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
bbox <- c(xmin = -120.5, ymin = 35.5, xmax = -120.0, ymax = 36.0)
buildings <- open_curtain("building", bbox)
sf::st_crs(buildings)
sf::st_bbox(buildings)
\dontshow{\}) # examplesIf}
}
