% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latest_overture_release.R
\name{latest_overture_release}
\alias{latest_overture_release}
\title{Discover the latest available Overture Maps release}
\usage{
latest_overture_release(conn = NULL, refresh = FALSE)
}
\arguments{
\item{conn}{A duckdb connection. Uses the cached session connection by
default.}

\item{refresh}{If \code{TRUE}, bypass the session cache and re-query the
catalog.}
}
\value{
A string identifying the latest release, e.g. \code{"2026-08-19.0"}.
}
\description{
\code{open_curtain()} needs a release date/version to build its S3 path (e.g.
\code{"2026-08-19.0"}). Overture cuts a new release roughly monthly, so hardcoding
one quickly goes stale. This queries Overture's STAC catalog
(\url{https://stac.overturemaps.org/catalog.json}) for its current \code{latest}
release, so callers don't have to track releases themselves or wait on a
package update. The result is cached for the session.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
latest_overture_release()
\dontshow{\}) # examplesIf}
}
