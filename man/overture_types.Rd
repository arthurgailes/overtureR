% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac.R
\name{overture_types}
\alias{overture_types}
\title{List the dataset types in an Overture release}
\usage{
overture_types(release = latest_overture_release(conn), conn = NULL)
}
\arguments{
\item{release}{An Overture release, such as \code{"2026-08-19.0"}. Defaults to
the latest release.}

\item{conn}{A duckdb connection. Uses the cached session connection by
default.}
}
\value{
A data frame with columns \code{type} and \code{theme}.
}
\description{
Reads the type-to-theme table for a release from Overture's STAC catalog,
so new types (such as \code{bathymetry}) appear without a package update. The
answer is cached per release. If the catalog can't be reached, the
package's built-in table is returned with a warning.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
overture_types()
\dontshow{\}) # examplesIf}
}
